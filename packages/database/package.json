{
  "name": "@shipradar/database",
  "description": "Database and authentication package",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./auth-client": "./src/auth-client.ts"
  },
  "scripts": {
    "auth:generate": "pnx auth@1.6.8 generate --output src/schema/auth.ts --yes",
    "db:drop": "drizzle-kit drop",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@better-auth/passkey": "1.6.8",
    "@neondatabase/serverless": "catalog:",
    "@shipradar/redis": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "17.2.3",
    "drizzle-orm": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "24.10.4",
    "drizzle-kit": "catalog:"
  }
}
